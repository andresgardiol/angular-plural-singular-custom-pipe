import { PluralSingularPipe } from './plural-singular.pipe';

describe('PluralSingularPipe', () => {
  it('create an instance', () => {
    const pipe = new PluralSingularPipe();
    expect(pipe).toBeTruthy();
  });
});
