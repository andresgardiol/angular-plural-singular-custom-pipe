import { ListSeparatorPipe } from './list-separator.pipe';

describe('ListSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ListSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
